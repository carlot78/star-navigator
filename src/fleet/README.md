# fleet/

The player fleet as a model: `ShipInstance` (a hull plus fitted weapons,
hull modifications, current damage and crew), refit rules (slot compatibility,
ordnance points), repairs, and salvage after battle. Pure logic, no scenes;
the refit screen lives under `ui/`. Arrives in milestone M5.
