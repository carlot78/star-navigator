# ui/theme/

The single Godot `Theme` resource the whole game uses (fonts, colours, touch
target sizes) plus reusable controls such as the virtual joystick. Every screen
inherits from the theme; no per-scene font or colour overrides outside
prototypes.
